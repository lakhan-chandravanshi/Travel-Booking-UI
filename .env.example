# Base URL of the Travel Booking API (no trailing slash).
# Local backend default:
VITE_API_URL=http://localhost:5000/api/v1

# When deployed, point this at your live API, e.g.
# VITE_API_URL=https://travel-booking-api.onrender.com/api/v1
